fun add_function(f, b)
    fun (f, b) inner(x)
        f(x) + b(x)
    end
    inner
end

fun add2(n)
    n + 2
end

fun mul2(n)
    n * 2
end

let f = add_function(add2, mul2);
let user_input = Integer(input())
print(f(user_input))